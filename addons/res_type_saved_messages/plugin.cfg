[plugin]

name="ResourceTypeSaved_Messages"
description="EditorPlugin that detects when any resource is saved (Watcher), sends message via EditorDebuggerPlugin \"session\" (Communicator), which can be received at runtime in scripts.
Used to automatically update shader via Util/ParentShaderUpdater."
author="Georg Sochurek"
version="0.1"
script="res_type_saved_watcher.gd"
