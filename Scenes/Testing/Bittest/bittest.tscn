[gd_scene load_steps=5 format=2]

[ext_resource path="res://Scenes/Testing/Bittest/bittest.png" type="Texture" id=1]

[sub_resource type="Shader" id=1]

code = "shader_type spatial;

uniform sampler2D tex;

void fragment () {
	vec3 color = texture(tex, UV).rgb;
	
	int full = (int(color.r * 255.0) << int(13)) + (int(color.g * 255.0) << int(5)) + (int(color.b * 255.0) >> int(3));
	int comp = int(1234567);
	
	if (full == comp) {
		color = vec3(0);
	}
	
	ALBEDO = color;
}"

[sub_resource type="ShaderMaterial" id=2]

render_priority = 0
shader = SubResource( 1 )
shader_param/tex = ExtResource( 1 )
_sections_unfolded = [ "shader_param" ]

[sub_resource type="PlaneMesh" id=3]

material = SubResource( 2 )
custom_aabb = AABB( 0, 0, 0, 0, 0, 0 )
size = Vector2( 2, 2 )
subdivide_width = 0
subdivide_depth = 0

[node name="MeshInstance" type="MeshInstance" index="0"]

layers = 1
material_override = null
cast_shadow = 1
extra_cull_margin = 0.0
use_in_baked_light = false
lod_min_distance = 0.0
lod_min_hysteresis = 0.0
lod_max_distance = 0.0
lod_max_hysteresis = 0.0
mesh = SubResource( 3 )
skeleton = NodePath("..")
material/0 = null


